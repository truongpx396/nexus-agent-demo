Eval corpus (`corpus/*.yaml`), runner, code graders, and the CI gate land in
Phase 1 as a skeleton (README.md §5, task 1.9 — the gate must exist *before*
the first behavior-bearing slice) and harden in Phase 9 (trial statistics,
suite classes, judge calibration, efficiency gating).
