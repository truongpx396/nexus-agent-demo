Property-based tests land in Phase 2: the paired `tool_use`/`tool_result`
invariant is a *total* invariant over all event histories, so it is
property-tested over generated sequences rather than sampled by examples
(README.md §4, task 2.5).
