Contract tests land alongside the seams they pin: the kernel ABI, the
control-plane <-> data-plane `v1` shapes, the run-API OpenAPI surface, and
the import-boundary assertion for the CP/DP split (README.md §4, §5 task 1.11).
